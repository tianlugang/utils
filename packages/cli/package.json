{
  "name": "@tlg/cli",
  "version": "1.1.0",
  "description": "my cli utils",
  "license": "MIT",
  "dependencies": {
    "@tlg/builder": "^1.1.0",
    "@tlg/utils": "^1.1.0",
    "commander": "^7.0.0"
  },
  "bin": {
    "tlg": "./bin/tlg"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf ./lib && rimraf *.tsbuildinfo && rimraf ./types"
  }
}
